# Copy to .env or .env.local and fill in values. Do not commit .env.

# --- Sanity (frontend: VITE_*; API/server: SANITY_*) ---
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_TOKEN=

# --- Stripe ---
VITE_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Payment webhook → ComputerEase sync (optional) ---
# Full app URL for internal webhook call. If unset, uses https://VERCEL_URL on Vercel.
# APP_URL=https://yourdomain.com

# --- Admin & Sync ---
# Dashboard login password (admin invoice-sync page)
DASHBOARD_PASSWORD=
# Sync API key for manual/external calls to computerease-import and csv-import
SYNC_API_KEY=
# Set in Vercel for cron; Vercel sends Authorization: Bearer CRON_SECRET when invoking cron
CRON_SECRET=

# --- n8n Chatbot (optional) ---
# n8n webhook URL (server-side only, called by /api/chatbot/chat proxy)
# Set this in Vercel (Project → Settings → Environment Variables) for Production
N8N_CHATBOT_WEBHOOK=https://n8n.srv1328675.hstgr.cloud/webhook/729c8053-809b-487f-b8ef-9d7f9e8e6452

# --- ComputerEase (optional) ---
# COMPUTEREASE_API_URL=
# COMPUTEREASE_API_USERNAME=
# COMPUTEREASE_API_PASSWORD=
# COMPUTEREASE_API_KEY=
# COMPUTEREASE_SYNC_ENABLED=true
# COMPUTEREASE_CSV_ENABLED=false
